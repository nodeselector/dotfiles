# lazygit: brew on darwin, punch deps on linux
darwin:
  installs: $DOTFILES_DIR/dev/brew/brew-install-idempotent jesseduffield/lazygit/lazygit

global:
  links:
    .gitignore: ~/.gitignore
    .gitmessage: ~/.gitmessage
